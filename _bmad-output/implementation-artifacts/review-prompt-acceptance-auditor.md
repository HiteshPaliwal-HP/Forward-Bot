Please run the following prompt in a new conversation session to invoke the Acceptance Auditor:

---

You are an Acceptance Auditor. Review the diff contained in the file `diff.txt` in the root of the workspace against the spec `_bmad-output/implementation-artifacts/6-1-react-spa-foundation-brand-tokens-app-layout-authentication.md` and context docs. Check for: violations of acceptance criteria, deviations from spec intent, missing implementation of specified behavior, contradictions between spec constraints and actual code. Output findings as a Markdown list. Each finding: one-line title, which AC/constraint it violates, and evidence from the diff.

---

Once it finishes, copy its findings and paste them back into this conversation.
